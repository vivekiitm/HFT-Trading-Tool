import core
import random

engine = core.MatchingEngine()

for _ in range(10000):
    price = random.uniform(100, 110)
    engine.process_order(True, price, 1)

print("Best Bid:", engine.ob.best_bid() if hasattr(engine, "ob") else "N/A")
